/**
 * Main Application Entry Point
 */
document.addEventListener('DOMContentLoaded', () => {
    // Initialize UI when DOM is ready
    ui.init();
    
    console.log('InventoryHouse Pro - Frontend Initialized');
});
